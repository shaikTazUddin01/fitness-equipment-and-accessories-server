import { Types } from "mongoose";

export type TCustomer = {
  // name: string;
  // email: string;
  // phoneNumber: string;
  // address: string;
  customerId:Types.ObjectId
  // orderId:Types.ObjectId
};
